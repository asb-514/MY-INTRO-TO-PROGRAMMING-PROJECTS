#include <stdio.h>
#include <stdlib.h>

int main()
{
    int number,count;
    printf("inout a number:");
    scanf("%d",&number);
    count = 0;
    while(count < count +1){
        count = count + 1;
        printf("%d %d = %d\n",number,count,number*count);
    }
    return 0;
}