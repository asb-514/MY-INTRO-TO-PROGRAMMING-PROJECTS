#include <stdio.h>
#include <stdlib.h>
int main()
{
   int a,b,c,d,e,f; // deaclaring variables 
   a=10;
   b=25;
   c=5;
   d=5;
   e=3;
   f=a+b+c+d+e; 

   printf("\n \t \t \t \t the sum of %d %d %d %d and %d is equal to %d \n " , a,b,c,d,e,f );

   return 0;
}/*Problem 2. Write a C program called sum.c that prints the sum of 5 integers in the format
shown in the example below. Use variables of type int. Try changing the values for each variable
and check if the output is correct.
Example: If the 5 integers you want to use are 10, 25, 5, 5, and 3, it should print
10 + 25 + 5 + 5 + 3 = 48*/